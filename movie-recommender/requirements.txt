# ============================================================
# requirements.txt — PINNED production dependencies
# Generated from pyproject.toml
# DO NOT EDIT MANUALLY — re-run pip-compile to update:
#   pip-compile pyproject.toml -o requirements.txt
#
# Verified compatible stack — March 2026
# Python: 3.11
# ============================================================

# ── core data ───────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4            # pinned to 1.26.x — broadest compat with all libs
pyarrow==15.0.2
scipy==1.13.1

# ── ML ──────────────────────────────────────────────────────
scikit-learn==1.5.2
xgboost==2.1.4           # pinned to 2.1.x — stable, avoids 3.x breaking changes
lightgbm==4.5.0          # 4.4+ for numpy 2.x compat; 4.5 is stable
implicit==0.7.2          # latest stable
faiss-cpu==1.8.0         # conservative pin — 1.13.x exists but new API changes

# ── MLflow ──────────────────────────────────────────────────
mlflow==2.14.3           # 2.x — avoids MLflow 3.x breaking changes
# mlflow transitive deps (critical ones pinned)
alembic==1.13.2
sqlalchemy==2.0.31
flask==3.0.3             # mlflow UI backend
gunicorn==22.0.0

# ── API ─────────────────────────────────────────────────────
fastapi==0.111.1
uvicorn==0.30.3
uvloop==0.19.0           # uvicorn[standard] dep — faster event loop
httptools==0.6.1         # uvicorn[standard] dep
starlette==0.37.2        # fastapi dep
pydantic==2.8.2
pydantic-core==2.20.1    # pydantic dep
anyio==4.4.0             # starlette/fastapi dep

# ── database / cache ────────────────────────────────────────
asyncpg==0.29.0
redis==5.0.8

# ── monitoring ──────────────────────────────────────────────
prometheus-client==0.20.0

# ── utilities ───────────────────────────────────────────────
click==8.1.7
pyyaml==6.0.2
python-dotenv==1.0.1
tqdm==4.66.5

# ── transitive deps (common conflict sources — pinned) ──────
packaging==24.1
certifi==2024.7.4
urllib3==2.2.2
requests==2.32.3