# ============================================================
# requirements.txt — PINNED production dependencies
# Python 3.12 | Windows (local dev) + Linux x86_64 (Docker)
#
# NOTE: uvloop and httptools excluded — Windows incompatible.
# They are only needed inside Docker (Linux). The Dockerfile
# installs them separately via uvicorn[standard].
# ============================================================

# ── core data ────────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4
pyarrow==15.0.2
scipy==1.13.1

# ── ML ───────────────────────────────────────────────────────
scikit-learn==1.5.2
xgboost==2.1.4
lightgbm==4.5.0
implicit==0.7.2
faiss-cpu==1.8.0.post1

# ── MLflow ───────────────────────────────────────────────────
mlflow==2.14.3
alembic==1.13.2
sqlalchemy==2.0.31

# ── API ──────────────────────────────────────────────────────
fastapi==0.111.1
uvicorn==0.30.3
pydantic==2.8.2
pydantic-core==2.20.1
starlette==0.37.2
anyio==4.4.0
python-multipart==0.0.9

# ── database / cache ─────────────────────────────────────────
asyncpg==0.29.0
redis==5.0.8

# ── monitoring ───────────────────────────────────────────────
prometheus-client==0.20.0

# ── utilities ────────────────────────────────────────────────
click==8.1.7
pyyaml==6.0.2
python-dotenv==1.0.1
tqdm==4.66.5
packaging==24.1
certifi==2024.7.4
urllib3==2.2.2
requests==2.32.3